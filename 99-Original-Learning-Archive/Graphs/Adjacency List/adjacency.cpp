#include <iostream>
#include <vector>
using namespace std;

int main() {
    int n = 5; // nodes

    vector<int> adj[5];

    adj[0].push_back(1);
    adj[0].push_back(2);

    adj[1].push_back(0);
}