# Recursion

A recursive solution must have:

- a base case
- progress toward the base case
- a recursive relation

## Progression

Simple recursion → recursion tree → multiple branches → backtracking → DP.

Draw the recursion tree when you do not understand the complexity.
