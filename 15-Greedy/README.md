# Greedy Algorithms

A greedy algorithm makes a locally optimal choice with the hope that the sequence of choices is globally optimal.

Do not use greedy merely because it is fast.

For each greedy problem, identify:

1. the greedy choice
2. why it is safe
3. the exchange/stays-ahead argument when applicable
4. the resulting complexity
