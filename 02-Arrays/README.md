# Arrays

## Core progression

Basics → traversal → searching → prefix sums → two pointers → sliding window → subarrays → sorting techniques → matrix problems → advanced problems.

## Essential patterns

- Prefix sum
- Difference array
- Two pointers
- Sliding window
- Hashing
- Kadane
- Binary search
- Dutch National Flag

## Questions to ask

- Is the input sorted?
- Is the answer about a contiguous range?
- Can a prefix sum remove repeated work?
- Can two pointers eliminate candidates?
- Is there a frequency/counting trick?
