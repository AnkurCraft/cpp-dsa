# Union-Find / DSU

Disjoint Set Union efficiently maintains connected components under merges.

Core operations:
- find
- union

With path compression and union by size/rank, operations are effectively near-constant amortized time.
