# Binary Search on Answer

Instead of searching an array, search a numeric answer space.

You need a monotonic feasibility predicate:

`false false false true true true`

Then binary search the first/last feasible value.
