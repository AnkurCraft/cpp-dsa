# Prefix Sum

Precompute cumulative information so range queries can be answered quickly.

For an array `a`, define `pref[i]` as the sum of elements before index `i`.

Then a range sum can be computed in O(1) after O(n) preprocessing.
